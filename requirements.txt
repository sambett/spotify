# Spotify Analytics Pipeline - Python Dependencies

# Environment & Configuration
python-dotenv==1.0.1          # Load environment variables from .env file

# HTTP Requests & API Communication
requests==2.32.4              # HTTP library for Spotify API calls
urllib3==2.2.3                # HTTP client (dependency of requests)

# SSL/TLS for HTTPS callback server
cryptography==43.0.0          # Generate self-signed certificates for localhost HTTPS

# Task Scheduling
schedule==1.2.0               # Simple job scheduling for automated data fetching

# Note: PySpark is pre-installed in apache/spark:3.5.3 image
# No need to install it via pip - use spark-submit instead

# Machine Learning & Data Science (Python 3.8 compatible versions)
numpy==1.24.4                 # Required by Spark MLlib and ML operations
scikit-learn==1.3.2           # ML utilities and metrics
pandas==2.0.3                 # Data manipulation for model evaluation
matplotlib==3.7.5             # Plotting and visualization
seaborn==0.13.0               # Statistical data visualization

# Future dependencies (uncomment when needed):
# retry==0.9.2                # Retry logic decorator (alternative to custom retry)
